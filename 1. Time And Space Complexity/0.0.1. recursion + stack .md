
তোমার Noteটা খুব ভালো হয়েছে, কিন্তু সত্যি বলতে **Recursion, Call Stack, O(2ⁿ), O(n!)—এই চারটা সবচেয়ে confusing topic** যথেষ্ট explain করা হয়নি। এগুলো না বুঝলে Dynamic Programming, Backtracking, Divide & Conquer, Tree, Graph ইত্যাদিতে Complexity বের করতে সমস্যা হবে।

আমি নিচের Section-গুলো **ঠিক এই Note-এর মধ্যে Add করার** পরামর্শ দেব।

---

# Recursion (Time + Space)

Recursion-এ **দুইটা Complexity আলাদা করে ভাবতে হবে।**

```
Time Complexity
↓

Function মোট কতবার Call হচ্ছে?
```

```
Space Complexity
↓

Call Stack-এ একসাথে কতগুলো Function আছে?
```

---

## Example-1 : Linear Recursion

```cpp
void fun(int n)
{
    if(n==0) return;

    fun(n-1);
}
```

Call হবে

```
fun(5)

↓

fun(4)

↓

fun(3)

↓

fun(2)

↓

fun(1)

↓

fun(0)
```

মোট Call

```
n
```

Time

```
O(n)
```

একসাথে Stack-এ থাকবে

```
n Calls
```

Space

```
O(n)
```

---

## Example-2 : Factorial

```cpp
int fact(int n)
{
    if(n==1) return 1;

    return n*fact(n-1);
}
```

Call

```
5

↓

4

↓

3

↓

2

↓

1
```

Time

```
O(n)
```

Space

```
O(n)
```

কারণ

```
Call Stack = n
```

---

## Example-3 : Binary Search (Recursive)

```cpp
binarySearch()
```

প্রতি Call-এ

```
Half হচ্ছে
```

Call হবে

```
log n
```

Time

```
O(log n)
```

Stack Depth

```
log n
```

Space

```
O(log n)
```

---

## Recursion Golden Rule

```
Time

↓

মোট Function Call
```

```
Space

↓

Maximum Call Stack Depth
```

---

# Stack (Space Complexity)

অনেকেই ভুল করে মনে করে Stack সবসময়

```
O(1)
```

আসলে Stack-এর Size-এর উপর নির্ভর করে।

---

Example

```cpp
stack<int> st;

for(int i=0;i<n;i++)
{
    st.push(i);
}
```

Stack-এ

```
n Elements
```

Time

```
Push সব মিলিয়ে

O(n)
```

Extra Space

```
O(n)
```

---

Example

```cpp
stack<int> st;

st.push(10);

st.pop();
```

এক সময়ে মাত্র

```
1 Element
```

Space

```
O(1)
```

---

## STL Stack Operation

| Operation | Time |
| --------- | ---- |
| push()    | O(1) |
| pop()     | O(1) |
| top()     | O(1) |
| empty()   | O(1) |

---

## Stack vs Call Stack

```
Your Stack<int>

↓

Programmer তৈরি করে
```

```
Call Stack

↓

Compiler Automatically তৈরি করে
```

দুটোরই Space Complexity Count করতে হয়।

---

# Exponential Complexity — O(2ⁿ)

এটাই সবচেয়ে Dangerous Complexity।

---

Example

```cpp
void solve(int n)
{
    if(n==0) return;

    solve(n-1);

    solve(n-1);
}
```

প্রতি Function

```
2টা নতুন Function Call করছে
```

Tree

```
             n
           /   \
        n-1   n-1
       /  \   /  \
     ...
```

Call সংখ্যা

```
1

↓

2

↓

4

↓

8

↓

16
```

অর্থাৎ

```
2^n
```

Time

```
O(2ⁿ)
```

Maximum Stack Depth

```
n
```

Space

```
O(n)
```

---

## কেন Fibonacci Slow?

```cpp
fib(n)
{
    fib(n-1);

    fib(n-2);
}
```

একই Value বারবার Calculate হচ্ছে।

Example

```
fib(5)

↓

fib(4)

↓

fib(3)

↓

fib(2)

↓

fib(3)

↓

fib(2)
```

অনেক Duplicate Call।

Time

```
O(2ⁿ)
```

DP ব্যবহার করলে

```
O(n)
```

---

# Factorial Complexity — O(n!)

Backtracking-এ সবচেয়ে বেশি দেখা যায়।

---

Example

```
ABC
```

Possible Arrangement

```
ABC

ACB

BAC

BCA

CAB

CBA
```

Total

```
3!
```

যদি

```
4 Character
```

হয়

```
4!

=

24
```

যদি

```
10 Character
```

হয়

```
10!

=

3,628,800
```

তাই

```
Time

=

O(n!)
```

---

## কোথায় O(n!) পাওয়া যায়?

* All Permutations
* Travelling Salesman (Brute Force)
* N-Queens (Brute Force)
* Backtracking-এর অনেক সমস্যা

---

# Complexity Growth Chart

|  n | log n |  n | n log n | n² |       2ⁿ |             n! |
| -: | ----: | -: | ------: | --: | --------: | -------------: |
|  5 |     3 |  5 |      12 |  25 |        32 |            120 |
| 10 |     4 | 10 |      33 | 100 |     1,024 |      3,628,800 |
| 20 |     5 | 20 |      86 | 400 | 1,048,576 | 2.43 × 10¹⁸ |

**Observation**

```
log n

↓

Slow Growth
```

```
n

↓

Linear
```

```
n²

↓

Acceptable for Small Input
```

```
2ⁿ

↓

Very Slow
```

```
n!

↓

Almost Impossible for Large n
```

---

# Recursion Tree Shortcut

Recursion দেখলে এই ২টা প্রশ্ন করো:

### 1.

```
একটি Function কয়টা নতুন Function Call করছে?
```

* 1 Call → সাধারণত O(n)
* 2 Call → সাধারণত O(2ⁿ)
* k Call → সাধারণত O(kⁿ)

---

### 2.

```
Recursion কত গভীর (Depth)?
```

* Depth = n → Space O(n)
* Depth = log n → Space O(log n)

---

# 🚀 Ultimate Memory Trick

```
1 Loop

↓

O(n)
```

```
Nested Loop

↓

Multiply

↓

O(n²)
```

```
Half Every Time

↓

O(log n)
```

```
One Recursive Call

↓

Time O(n)

Space O(n)
```

```
Two Recursive Calls

↓

Time O(2ⁿ)

Space O(n)
```

```
Permutation / Backtracking

↓

O(n!)
```

```
Extra Array / Vector / Queue / Stack

↓

Space O(n)
```

```
Only Variables

↓

Space O(1)
```

---

এগুলো যোগ করলে তোমার Note-টা **Beginner থেকে Interview Level** হয়ে যাবে। বিশেষ করে **Recursion Tree**, **Call Stack vs Stack**, **O(2ⁿ)** এবং **O(n!)**-এর ব্যাখ্যাগুলো ভবিষ্যতে Dynamic Programming, Backtracking ও Graph/Tree Algorithm শেখার জন্য শক্ত ভিত্তি তৈরি করবে।



=============================================================================================



# 🚀 Ultimate Time & Space Complexity Master Note (Recursion + Recurrence + Loop Patterns)

> **Goal:** এই Note শেষ করলে তুমি **Loop, Recursion, Divide & Conquer, DP, Backtracking**—সব Algorithm-এর **Time & Space Complexity** নিজে নিজে বের করতে পারবে।

---

# 🔥 The Golden Rule

```
Time Complexity
↓

CPU কত কাজ করছে?

=

Total Operations
```

```
Space Complexity
↓

Input বাদ দিয়ে

Extra Memory কত লাগছে?

=

Variables
+
Arrays
+
Queue
+
Stack
+
Call Stack
```

---

# STEP-1 : প্রথমে এই ৫টা Question করো

```
1. Loop আছে?

↓

Count Iteration
```

```
2. Recursion আছে?

↓

Count Function Calls
```

```
3. Divide করছে?

↓

Master Theorem / Recurrence
```

```
4. Extra Memory নিচ্ছে?

↓

Space Count করো
```

```
5. Call Stack কত গভীর?

↓

Recursion Depth
```

---

# STEP-2 : সবচেয়ে Common Patterns

| Pattern             | Time       |
| ------------------- | ---------- |
| Constant            | O(1)       |
| One Loop            | O(n)       |
| Two Nested Loop     | O(n²)     |
| Three Nested Loop   | O(n³)     |
| Half Every Time     | O(log n)   |
| Divide + Merge      | O(n log n) |
| Two Recursive Calls | O(2ⁿ)     |
| All Permutations    | O(n!)      |

---

# STEP-3 : Recursion-এর Golden Law

Recursion-এ **দুইটা জিনিস আলাদা করে বের করতে হবে।**

```
Time

↓

মোট Function Call
```

```
Space

↓

Maximum Call Stack Depth
```

এটাই সবচেয়ে Important Rule।

---

# Pattern-1 : Linear Recursion

Formula

```
T(n)=T(n-1)+O(1)
```

Example

```cpp
void solve(int n)
{
    if(n==0) return;

    solve(n-1);
}
```

Call Tree

```
solve(5)

↓

solve(4)

↓

solve(3)

↓

solve(2)

↓

solve(1)

↓

solve(0)
```

মোট Call

```
n
```

Time

```
O(n)
```

Call Stack

```
5

↓

4

↓

3

↓

2

↓

1
```

একসাথে Stack-এ

```
n Call
```

Space

```
O(n)
```

---

# Pattern-2 : Linear Work প্রতি Call

Formula

```
T(n)=T(n-1)+O(n)
```

Example

```cpp
void solve(int n)
{
    if(n==0) return;

    for(int i=0;i<n;i++)
    {

    }

    solve(n-1);
}
```

Work হচ্ছে

```
n

+

n-1

+

n-2

+

...

+

1
```

যোগফল

```
n(n+1)/2
```

Time

```
O(n²)
```

Call Stack

```
n
```

Space

```
O(n)
```

---

# Pattern-3 : Divide by Half

Formula

```
T(n)=T(n/2)+O(1)
```

Example

```cpp
binarySearch()
```

প্রতি Call

```
Half হচ্ছে
```

Example

```
1024

↓

512

↓

256

↓

128
```

Call হবে

```
log₂ n
```

Time

```
O(log n)
```

Call Stack

```
log n
```

Space

```
O(log n)
```

---

# Pattern-4 : Divide by Half + Linear Work

Formula

```
T(n)=T(n/2)+O(n)
```

Example

```cpp
Process Whole Array

↓

Then

Half Problem Solve
```

Time

```
n+n/2+n/4+...

≈2n
```

Answer

```
O(n)
```

Space

```
O(log n)
```

কারণ

```
Depth = log n
```

---

# Pattern-5 : Merge Sort

Formula

```
T(n)=2T(n/2)+O(n)
```

মানে

```
Half

+

Half

+

Merge
```

Tree

```
            n

       n/2     n/2

   n/4 n/4 n/4 n/4
```

প্রতিটি Level-এর Cost

```
n
```

Level সংখ্যা

```
log n
```

Time

```
O(n log n)
```

Extra Array লাগে

```
O(n)
```

Call Stack

```
O(log n)
```

Total Auxiliary Space (সাধারণভাবে বলা হয়)

```
O(n)
```

---

# Pattern-6 : Quick Sort

Formula

```
T(n)=T(k)+T(n-k-1)+O(n)
```

Average

```
Balanced
```

Time

```
O(n log n)
```

Worst

```
One Side Empty
```

Time

```
O(n²)
```

Space

Average

```
O(log n)
```

Worst

```
O(n)
```

---

# Pattern-7 : Fibonacci

Formula

```
T(n)=T(n-1)+T(n-2)+O(1)
```

Tree

```
fib(5)

↓

fib(4)

↓

fib(3)

↓

fib(2)

↓

fib(3)

↓

fib(2)
```

একই কাজ বারবার হচ্ছে।

Time

```
O(2ⁿ)
```

Call Stack Depth

```
n
```

Space

```
O(n)
```

DP করলে

Time

```
O(n)
```

Space

```
O(n)

or

O(1)
```

---

# Pattern-8 : Two Recursive Calls

Formula

```
T(n)=2T(n-1)+O(1)
```

Tree

```
           n

      n-1     n-1

   n-2 n-2 n-2 n-2
```

Nodes

```
1

↓

2

↓

4

↓

8

↓

16
```

Time

```
O(2ⁿ)
```

Maximum Stack

```
Only One Path
```

```
n
```

Space

```
O(n)
```

⚠️ মনে রাখো:

```
Time

≠

Space
```

যদিও

```
2ⁿ Call
```

হয়

Stack একসাথে

```
2ⁿ
```

হয় না।

কারণ

এক Path শেষ হলে

Stack Pop হয়ে যায়।

---

# Pattern-9 : Backtracking / Permutation

Formula

```
T(n)=n*T(n-1)
```

মানে

```
প্রথমে

n Choice

↓

তারপর

n-1 Choice

↓

n-2
```

Total

```
n!
```

Example

```
ABC
```

Permutation

```
ABC

ACB

BAC

BCA

CAB

CBA
```

Time

```
O(n!)
```

Recursion Depth

```
n
```

Space

```
O(n)
```

---

# Pattern-10 : n log n কোথা থেকে আসে?

এটাই সবচেয়ে Important।

```
প্রতি Level

↓

O(n)
```

```
Level সংখ্যা

↓

log n
```

তাই

```
n × log n
```

=

```
O(n log n)
```

Examples

```
Merge Sort

Heap Sort

Balanced Quick Sort

Segment Tree Build

FFT
```

---

# Master Recurrence Cheat Sheet

| Recurrence         | Time       | Space    |
| ------------------ | ---------- | -------- |
| T(n)=T(n-1)+O(1)   | O(n)       | O(n)     |
| T(n)=T(n-1)+O(n)   | O(n²)     | O(n)     |
| T(n)=T(n/2)+O(1)   | O(log n)   | O(log n) |
| T(n)=T(n/2)+O(n)   | O(n)       | O(log n) |
| T(n)=2T(n/2)+O(n)  | O(n log n) | O(n)*    |
| T(n)=T(n-1)+T(n-2) | O(2ⁿ)     | O(n)     |
| T(n)=2T(n-1)+O(1)  | O(2ⁿ)     | O(n)     |
| T(n)=nT(n-1)       | O(n!)      | O(n)     |

> * Merge Sort-এর Auxiliary Space `O(n)` (merge buffer-এর জন্য), Call Stack `O(log n)`।

---

# Space Complexity Laws

```
Only Variables

↓

O(1)
```

---

```
Array[n]

↓

O(n)
```

---

```
Matrix[n][n]

↓

O(n²)
```

---

```
Queue

Worst

↓

O(n)
```

---

```
Stack

Worst

↓

O(n)
```

---

```
HashMap

Worst

↓

O(n)
```

---

```
Recursion

↓

Maximum Call Stack
```

---

# Call Stack Law

সবচেয়ে বেশি ভুল এখানেই হয়।

```
Time

=

Total Function Call
```

```
Space

=

Maximum Stack Height
```

Example

Fibonacci

```
Time

2ⁿ
```

কিন্তু

```
Space

n
```

কারণ

```
এক সময়ে

একটাই Path

Memory-তে থাকে।
```

---

# Complexity Ranking (Best → Worst)

```
O(1)

↓

O(log n)

↓

O(n)

↓

O(n log n)

↓

O(n²)

↓

O(n³)

↓

O(2ⁿ)

↓

O(n!)
```

---

# 🔥 Ultimate Memory Rules (Interview Edition)

```
1 Loop

↓

O(n)
```

```
Nested Loop

↓

Multiply
```

```
Sequential Loop

↓

Add

↓

Keep Bigger One
```

```
Half Every Time

↓

O(log n)
```

```
One Recursive Call

↓

T(n)=T(n-1)+O(1)

↓

Time O(n)

Space O(n)
```

```
One Recursive Call + Linear Work

↓

T(n)=T(n-1)+O(n)

↓

Time O(n²)

Space O(n)
```

```
Half Recursion

↓

T(n)=T(n/2)+O(1)

↓

Time O(log n)

Space O(log n)
```

```
Divide + Merge

↓

T(n)=2T(n/2)+O(n)

↓

Time O(n log n)

Space O(n)
```

```
Two Recursive Calls

↓

T(n)=2T(n-1)+O(1)

↓

Time O(2ⁿ)

Space O(n)
```

```
Fibonacci

↓

T(n)=T(n-1)+T(n-2)

↓

Time O(2ⁿ)

Space O(n)
```

```
Permutation / Backtracking

↓

T(n)=nT(n-1)

↓

Time O(n!)

Space O(n)
```

```
Extra Array / Vector / Queue / Stack

↓

Count Elements

↓

Space O(n)
```

```
Only Variables

↓

Space O(1)
```

---

# 🎯 The "Never Forget Again" Formula

প্রতিটি Algorithm দেখে শুধু এই ৪টি প্রশ্ন করো:

1. **একটি Function থেকে কয়টি নতুন Function Call হচ্ছে?** (1, 2, n?)
2. **Problem Size কীভাবে কমছে?** (`n-1`, `n/2`, নাকি অন্যভাবে?)
3. **প্রতি Call-এ Extra Work কত?** (`O(1)`, `O(n)`, `O(log n)`?)
4. **একসাথে Maximum কতগুলো Function Stack-এ থাকবে?** (এটাই Space Complexity)

এই ৪টি প্রশ্নের উত্তর দিতে পারলে Loop, Recursion, Divide & Conquer, Binary Search, Merge Sort, Quick Sort, Backtracking, Dynamic Programming—প্রায় সব Algorithm-এর **Time ও Space Complexity** নিজেই বের করতে পারবে।
